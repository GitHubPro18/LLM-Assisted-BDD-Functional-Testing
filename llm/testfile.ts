// In a test file or console
/*
import { GeneratedScenario } from './scenario_generator';
import * as fs from 'fs';
const result = GeneratedScenario("User can login with valid credentials", 
                               ["open page", "login", "see message"], 
                               fs.readFileSync('llm/prompt_template.txt', 'utf8'));
console.log(result.prompt); // Verify interpolation
*/